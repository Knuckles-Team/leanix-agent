{
  "mcpServers": {
    "leanix": {
      "command": "leanix-mcp",
      "args": ["--transport", "stdio"],
      "env": {
        "LEANIX_URL": "${LEANIX_URL:-http://localhost:8080}",
        "LEANIX_TOKEN": "${LEANIX_TOKEN}"
      }
    }
  }
}

##  Next Steps
After configuring relation mappings:
  * Run a manual synchronization to test the configuration. To do this, on the configuration page, choose the three-dot icon > Run Sync.
  * Verify relations appear correctly in both systems.
  * Monitor the synchronization results and adjust mappings as needed.


YesNo
Send

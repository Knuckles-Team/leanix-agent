##  Running a Processor
After successfully testing a processor, you can run it. This applies changes to your workspace, so always ensure to test your processors first.
To run a processor, click Run at the bottom of the processor page. The results appear in the Output Log section.
